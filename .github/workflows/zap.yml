name: zap

# OWASP ZAP DAST scanning
# Secrets: GITHUB_TOKEN (auto)
# Inputs: target-url, scan-type (default: baseline), fail-on (default: HIGH)

on:
  workflow_call:
    inputs:
      target-url:
        required: true
        type: string
      scan-type:
        required: false
        default: "baseline"
        type: string
      fail-on:
        required: false
        default: "HIGH"
        type: string

permissions:
  contents: read
  security-events: write

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: OWASP ZAP baseline
        if: ${{ inputs.scan-type == 'baseline' }}
        uses: zaproxy/action-baseline@v0.15.0
        with:
          target: ${{ inputs.target-url }}
          fail_action: true
          allow_issue_writing: false
          issue_title: "ZAP Scan Report"
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: OWASP ZAP full
        if: ${{ inputs.scan-type == 'full' }}
        uses: zaproxy/action-full-scan@v0.13.0
        with:
          target: ${{ inputs.target-url }}
          fail_action: true
          allow_issue_writing: false
          issue_title: "ZAP Scan Report"
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: OWASP ZAP api
        if: ${{ inputs.scan-type == 'api' }}
        uses: zaproxy/action-api-scan@v0.10.0
        with:
          target: ${{ inputs.target-url }}
          fail_action: true
          allow_issue_writing: false
          issue_title: "ZAP Scan Report"
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload SARIF
        if: always()
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: zap-${{ inputs.scan-type }}.sarif
