name: nuclei

# ProjectDiscovery nuclei vulnerability scanner
# Inputs: target-url, severity (default: medium,high,critical), rate-limit (default: 150)

on:
  workflow_call:
    inputs:
      target-url:
        required: true
        type: string
      severity:
        required: false
        default: "medium,high,critical"
        type: string
      rate-limit:
        required: false
        default: 150
        type: number

permissions:
  contents: read
  security-events: write

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Nuclei scan
        uses: projectdiscovery/nuclei-action@cc153d0541e1adf8a42bbe31c0a4fb2376147538
        with:
          target: ${{ inputs.target-url }}
          severity: ${{ inputs.severity }}
          rate-limit: ${{ inputs.rate-limit }}
          sarif-export: nuclei.sarif

      - name: Upload SARIF
        if: always()
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: nuclei.sarif
